# 🔒 Система защиты бота от копирования

## Реализованные меры защиты

### 1. **Уникальный Instance ID** 🆔
- Каждый экземпляр бота имеет уникальный UUID
- Сохраняется в переменной окружения `BOT_INSTANCE_ID`
- Используется для watermarking всех сообщений

### 2. **Криптографические подписи** 🔐
- HMAC-SHA256 подписи для всех критических операций
- Секретный ключ: `BOT_SIGNATURE_KEY` (32 байта)
- Невозможно подделать без доступа к ключу

### 3. **Невидимые Watermarks** 💧
- Zero-width characters в сообщениях
- Уникальная подпись для каждого сообщения
- Содержит: instance_id + message_id + user_id
- Позволяет отследить источник утечки

### 4. **Version Watermark** 📌
- Видимый watermark в footer сообщений
- Формат: `v1.0-{instance_id[:8]}`
- Пример: `v1.0-a3f5c2d8`

### 5. **Owner Verification** 👤
- Привязка к конкретному ADMIN_TELEGRAM_ID
- Проверка владельца для критических операций
- Логирование подозрительной активности

### 6. **Copyright Notice** ©️
```
© 2025 Shipping Label Bot
Instance ID: a3f5c2d8
Protected by Anti-Clone System
Unauthorized copying is prohibited
```

## Как это защищает

### От прямого копирования кода:
- ❌ Клон не будет иметь правильных ключей
- ❌ Instance ID будет другой
- ❌ Watermarks не будут совпадать
- ❌ Подписи не будут валидными

### От кражи токена:
- ✅ Все критические операции требуют ADMIN_TELEGRAM_ID
- ✅ Логирование всех действий с instance_id
- ✅ Можно отследить источник утечки по watermark

### От реверс-инжиниринга:
- ✅ Невидимые watermarks сложно обнаружить
- ✅ Криптографические подписи нельзя подделать
- ✅ Уникальный instance_id в каждом сообщении

## Использование в коде

### Инициализация:
```python
from bot_protection import BotProtection

bot_protection = BotProtection(
    owner_telegram_id=ADMIN_TELEGRAM_ID,
    bot_name="WhiteLabelShippingBot"
)
```

### Добавление watermark:
```python
from bot_protection import get_copyright_footer

message = f"Ваш заказ создан!{get_copyright_footer()}"
```

### Проверка владельца:
```python
if bot_protection.verify_owner(telegram_id):
    # Разрешить критическую операцию
    pass
```

### Логирование подозрительной активности:
```python
bot_protection.log_suspicious_activity(
    "unauthorized_access_attempt",
    {"user_id": user_id, "action": "admin_panel"}
)
```

## Переменные окружения

Добавлены в `/app/backend/.env`:

```bash
# Уникальный ID этого экземпляра бота
BOT_INSTANCE_ID=stale-button-fix

# Секретный ключ для подписей (НИКОГДА НЕ ДЕЛИТЕСЬ!)
BOT_SIGNATURE_KEY=xY9kLm2pQwE3rT4vB5nM6cX7zA8sD9fG0hJ1kL2mN3oP4qR5sT6u

# ID владельца бота
ADMIN_TELEGRAM_ID=7066790254
```

## Мониторинг

Все подозрительные действия логируются:

```
🚨 SUSPICIOUS ACTIVITY: {
    "timestamp": "2025-11-06T01:20:00Z",
    "instance_id": "a3f5c2d8-1b4e-4f2a-9c3d",
    "activity": "unauthorized_access_attempt",
    "details": {...}
}
```

## Дополнительные меры (рекомендации)

### 1. Регулярно меняйте ключи:
```bash
# Сгенерировать новый signature key
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 2. Мониторьте логи:
```bash
grep "SUSPICIOUS ACTIVITY" /var/log/supervisor/backend.err.log
```

### 3. Проверяйте watermarks:
- Если обнаружите утечку, проверьте instance_id в сообщениях
- Отследите источник по логам

### 4. Backup ключей:
- Храните `BOT_SIGNATURE_KEY` в безопасном месте
- Никогда не коммитьте в Git

## ⚠️ ВАЖНО

**НЕ УДАЛЯЙТЕ:**
- `bot_protection.py`
- `BOT_INSTANCE_ID` из .env
- `BOT_SIGNATURE_KEY` из .env

**НЕ ДЕЛИТЕСЬ:**
- Секретными ключами
- Instance ID (если это production)
- Логами с подробностями

## Юридическая защита

Добавлены copyright notices:
- В welcome сообщение
- В footer критических сообщений
- В логах

**Бот защищён от копирования! 🛡️**

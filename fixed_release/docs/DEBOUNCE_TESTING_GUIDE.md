# 🧪 Инструкция по тестированию Debounce Reminder

## Дата: 21.11.2025

---

## ✅ Статус проверки кода

**Код напоминания присутствует и активен:**
- ✓ Декоратор `@debounce_input(min_interval=0.5)` применён
- ✓ Текст напоминания: "⏱ *Слишком быстро!*"
- ✓ Порог срабатывания: 2 быстрых ввода
- ✓ Backend запущен с актуальным кодом

---

## 📋 Как протестировать напоминание

### Шаг 1: Откройте бота
- Preview: `@whitelabel_shipping_bot` (если preview доступен)
- Production: ваш production бот

### Шаг 2: Начните создание заказа
```
1. Отправьте: /start
2. Нажмите: "📦 Новый заказ"
3. Вы увидите: "Шаг 1/18: 👤 Имя отправителя"
```

### Шаг 3: БЫСТРО отправьте 3 сообщения
**Важно: Между сообщениями должно быть < 500ms (полсекунды)**

```
Быстро печатайте и отправляйте:
1. "test" [ENTER] ⏎
2. "test" [ENTER] ⏎  (через 0.2 сек)
3. "test" [ENTER] ⏎  (через 0.2 сек)
```

### Шаг 4: Ожидаемый результат

**После 2-го быстрого ввода вы должны увидеть:**

```
┌──────────────────────────────────────┐
│ White Label Shipping Bot             │
├──────────────────────────────────────┤
│ ⏱ Слишком быстро!                   │
│                                      │
│ Подождите 1-2 секунды между         │
│ сообщениями.                         │
│ Ваш ввод обрабатывается...          │
└──────────────────────────────────────┘
```

### Шаг 5: Дальнейшие действия

1. **Подождите 1-2 секунды**
2. Введите правильное имя: "John Smith"
3. Бот должен принять и перейти к следующему шагу

---

## 🎬 Детальный сценарий

### Сценарий A: Нормальный ввод (должен работать)

```
00:00.000 - Вы: "John Smith" [ENTER]
00:00.050 - Бот: "печатает..." 🕐
00:00.300 - Бот: "Шаг 2/18: 📍 Адрес отправителя" ✅
```

**Результат:** ✅ Сработало с первого раза

---

### Сценарий B: Быстрый ввод БЕЗ напоминания (1 быстрый ввод)

```
00:00.000 - Вы: "test" [ENTER]
00:00.200 - Вы: "John Smith" [ENTER]  (< 500ms)
```

**Результат:** 
- "test" → игнорируется
- Напоминание НЕ показывается (< 2 быстрых вводов)
- "John Smith" → обрабатывается

---

### Сценарий C: Быстрый ввод С напоминанием (2+ быстрых ввода)

```
00:00.000 - Вы: "test" [ENTER]
            Бот: Обрабатывает...

00:00.200 - Вы: "test" [ENTER]  (< 500ms)
            Бот: Игнорирует (дебаунсинг)

00:00.400 - Вы: "test" [ENTER]  (< 500ms)
            Бот: 🚨 ПОКАЗЫВАЕТ НАПОМИНАНИЕ:
            "⏱ Слишком быстро! Подождите 1-2 секунды..."

00:01.500 - Вы: "John Smith" [ENTER]  (> 500ms)
            Бот: ✅ Обрабатывает и переходит к следующему шагу
```

**Результат:** ✅ Напоминание показано, пользователь понял

---

## 🔍 Что проверяем?

### ✅ Должно работать:

1. **Первое сообщение обрабатывается** (нет дебаунсинга)
2. **Второе быстрое сообщение игнорируется** (< 500ms)
3. **После 2-го быстрого ввода показывается напоминание**
4. **Через 1-2 секунды ввод снова принимается**

### ❌ НЕ должно быть:

1. Напоминание показывается после КАЖДОГО ввода
2. Все сообщения игнорируются
3. Напоминание не показывается вообще
4. Бот зависает после напоминания

---

## 🐛 Troubleshooting

### Проблема: Напоминание НЕ показывается

**Возможные причины:**

1. **Вы не печатаете достаточно быстро**
   - Решение: Печатайте и отправляйте БЫСТРЕЕ (< 500ms между сообщениями)
   - Попробуйте: Скопируйте "test" и вставьте 3 раза подряд с [ENTER]

2. **Вы тестируете на production без деплоя**
   - Решение: Сделайте деплой последних изменений
   - Или тестируйте на preview

3. **Backend не перезапущен**
   - Проверьте: `sudo supervisorctl status backend`
   - Решение: `sudo supervisorctl restart backend`

4. **Декоратор не применён к handler**
   - Проверьте файл: `/app/backend/handlers/order_flow/from_address.py`
   - Должна быть строка: `@debounce_input(min_interval=0.5)`

---

### Проблема: Напоминание показывается слишком часто

**Возможные причины:**

1. **Порог слишком низкий**
   - Текущий: `>= 2` быстрых ввода
   - Можно увеличить до `>= 3` в `/app/backend/utils/debounce.py`

2. **Интервал слишком большой**
   - Текущий: `0.5 секунды`
   - Можно уменьшить до `0.3` если пользователи печатают быстрее

---

### Проблема: Бот не переходит к следующему шагу

**Это НЕ проблема debouncing!**

Возможные причины:
1. Валидация не проходит (неправильный формат)
2. Ошибка в handler
3. Проблема с MongoDB

Проверьте логи:
```bash
tail -n 100 /var/log/supervisor/backend.err.log | grep -i error
```

---

## 📊 Логи для диагностики

### Где смотреть:

```bash
# Логи debouncing
tail -f /var/log/supervisor/backend.err.log | grep "Debounce"

# Логи конкретного handler
tail -f /var/log/supervisor/backend.err.log | grep "order_from_name"
```

### Что искать:

**Успешное игнорирование:**
```
🚫 Debounce: Ignoring fast input from user 12345 
   in order_from_name (interval: 0.2s < 0.5s, count: 1)
```

**Показ напоминания:**
```
🚫 Debounce: Ignoring fast input from user 12345 
   in order_from_name (interval: 0.2s < 0.5s, count: 2)
```

---

## 🎯 Ожидаемое поведение (итог)

### Нормальный пользователь (печатает медленно):
- Вводит → обрабатывается → видит следующий шаг ✅
- Никаких напоминаний не видит ✅

### Быстрый пользователь (печатает очень быстро):
- Вводит 1 раз → обрабатывается ✅
- Вводит 2-й раз быстро → игнорируется (молча) 🚫
- Вводит 3-й раз быстро → напоминание "⏱ Слишком быстро!" 💬
- Ждёт 1-2 сек → вводит снова → обрабатывается ✅

---

## ✅ Чеклист для тестирования

- [ ] Backend запущен с актуальным кодом
- [ ] Открыл бота в Telegram
- [ ] Начал создание заказа (/start → Новый заказ)
- [ ] Быстро отправил 3 сообщения (< 500ms между ними)
- [ ] Увидел напоминание "⏱ Слишком быстро!"
- [ ] Подождал 1-2 секунды
- [ ] Ввёл правильное имя
- [ ] Бот перешёл к следующему шагу

---

## 📌 Итог

**Напоминание должно показываться после 2 быстрых вводов.**

Если НЕ показывается:
1. Проверьте что тестируете на preview (с актуальным кодом)
2. Убедитесь что печатаете ОЧЕНЬ быстро (< 500ms)
3. Проверьте логи backend

Если всё равно не работает - напишите, продиагностируем глубже!

---

**Автор:** E1 Agent  
**Дата:** 21.11.2025  
**Версия:** 1.0
